import React from 'react';

const CartWidget = () => {
  return (
    <div className="cart-widget">
      {/* Contenido del carrito, como un ícono de carrito */}
      <span>🛒</span>
    </div>
  );
};

export default CartWidget;
